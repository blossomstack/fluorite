{%- if !doc.is_empty() %}
/**
{%- for line in doc %}
 {{ line }}
{%- endfor %}
 */
{%- endif %}
export enum {{ name }} {
{%- for variant in variants %}
  {{ variant }} = "{{ variant }}",
{%- endfor %}
}
